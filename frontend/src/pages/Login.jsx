/**
 * Composant Login (page de connexion).
 *
 * Ce composant affiche une structure de base pour une future page de connexion.
 *
 * @component
 * @returns {JSX.Element} Un conteneur avec un titre et un texte de placeholder.
 */

const Login = () => {
  return (
    <div className="p-6">
      <h2 className="text-xl font-bold mb-4">Connexion</h2>
      <p>Formulaire à venir...</p>
    </div>
  );
};

export default Login;
